import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function ProjectsPage() {
  return (
    <PlaceholderPage
      eyebrow="Projects"
      title="Project cards and case studies will plug into this route."
      description="The primitives are in place so later work can focus on content hierarchy, imagery, and interaction rather than base scaffolding."
    />
  );
}
