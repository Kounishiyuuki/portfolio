import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function ContactPage() {
  return (
    <PlaceholderPage
      eyebrow="Contact"
      title="A dedicated contact page can be layered in next."
      description="When the screenshot arrives, we can add form layout, availability status, and supporting detail without revisiting the shared shell."
    />
  );
}
